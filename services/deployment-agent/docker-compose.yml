version: "3.8"
services:
  auto-deployment-agent:
    image: ${DOCKER_REGISTRY}/deployment-agent:0.10.2
    init: true
    ports:
      - "8888"
    configs:
      - source: deployment_config
        target: /home/scu/config-prod.yaml
    networks:
      - portainer_agent_network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.role == manager]
networks:
  portainer_agent_network:
    external: true

configs:
  deployment_config:
    file: ./${DEPLOYMENT_AGENT_CONFIG}
