services:
  api-server:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  webserver:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  storage:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  rabbit:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  redis:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  sidecar:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar==true
          - node.role == worker
  traefik:
    deploy:
      placement:
        constraints:
          - node.labels.traefik==true
  whoami:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker
  traefik_api:
    deploy:
      placement:
        constraints:
          - node.labels.sidecar!=true
          - node.role == worker